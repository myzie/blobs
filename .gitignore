# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib

jwt.key
jwt.key.pub
cmd/blobsuser/blobsuser

# Test binary, build with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out

blobs
